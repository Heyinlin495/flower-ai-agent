# ================================
# 花卉识别 AI Agent - Python 依赖
# ================================
# 版本已锁定（== 精确到本地 venv 实测版本）：langchain 1.x 等 API 变动频繁，
# 锁定后本地/Docker/CI 行为一致，避免"能跑但装不上/跑崩"。

# Web 框架
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# 前端（Streamlit）
streamlit==1.60.0
requests==2.34.2

# LangChain 1.x（代码使用 create_agent / langgraph API）
langchain==1.3.14
langchain-core==1.5.3
langchain-community==0.4.2
langchain-openai==1.4.1
langchain-text-splitters==1.1.2

# 阿里云 DashScope
dashscope==1.26.5
openai==2.52.0

# 向量数据库
chromadb==1.5.9
faiss-cpu==1.14.3

# 阿里云 OSS
oss2==2.19.1

# 数据模型
pydantic==2.13.4
pydantic-settings==2.14.2

# 图片处理（上传压缩：backend/image_utils.py + 前端缩略图）
pillow==12.3.0

# 文档处理
pypdf==6.14.2

# 工具库
python-dotenv==1.2.2
aiofiles==25.1.0
httpx==0.28.1
numpy==2.5.1

# 测试（全离线：stub Agent + 假 Embedding，CI 无需 API Key）
pytest==9.1.1
pytest-asyncio==1.4.0
